name: parse_args --exclude 多次累积成逗号分隔
points: 2
run: |
  source "$EXERCISE_DIR/starter/lib/cli.sh"
  parse_args --exclude=.git '--exclude=*.tmp'
expect:
  exit: 0
  stdout: |
    DEBUG=0
    EXCLUDES=.git,*.tmp
    POS=
