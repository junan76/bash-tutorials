name: filter_match 区分匹配与不匹配（含正例对照）
points: 1
run: |
  source "$EXERCISE_DIR/starter/lib/filter.sh"
  filter_match "yes.tmp" "*.tmp"            && echo positive-ok
  filter_match "main.c"  "*.tmp" "*.log"    || echo negative-ok
expect:
  exit: 0
  stdout: |
    positive-ok
    negative-ok
