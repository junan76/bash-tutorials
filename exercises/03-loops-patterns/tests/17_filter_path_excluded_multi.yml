name: filter_path_excluded 多 exclude 任一命中
points: 1
run: |
  source "$EXERCISE_DIR/starter/lib/filter.sh"
  filter_path_excluded "node_modules/foo/bar.js" "*.tmp" ".git" "node_modules"
expect:
  exit: 0
