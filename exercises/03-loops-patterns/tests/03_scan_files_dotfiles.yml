name: scan_files 包含隐藏文件（dotglob）
points: 1
setup: |
  mkdir src
  touch src/.hidden src/visible.txt
run: |
  source "$EXERCISE_DIR/starter/lib/scan.sh"
  scan_files src
expect:
  exit: 0
  stdout: |
    .hidden
    visible.txt
