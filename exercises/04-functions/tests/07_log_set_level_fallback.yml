name: log_set_level 非法值回退到 info（log_info 仍能正常打印）
points: 1
run: |
  source "$EXERCISE_DIR/starter/lib/log.sh"
  log_set_level garbage 2>/dev/null || true
  log_info "after-fallback"
expect:
  exit: 0
  stderr_contains: "[INFO] after-fallback"
