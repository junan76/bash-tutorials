name: prune_snapshots 空目录 no-op rc=0
points: 1
run: |
  source "$EXERCISE_DIR/starter/lib/maint.sh"
  mkdir snaps
  prune_snapshots snaps 3
  rc=$?
  echo "rc=$rc"
expect:
  exit: 0
  stdout: |
    rc=0
